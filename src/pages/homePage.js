import React from 'react';

const HomePage = (props) => {
    const {
    } = props;

    const rootDOM = React.useRef(); // current container ref
    
    React.useEffect(() => {
        
    }, []);

};

export default HomePage;